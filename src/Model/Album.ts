// todo this is just a placeholder to display _something_
export class Album {
    id: number;
    name: string;

    constructor(id: number, name: string) {
        this.id = id;
        this.name = name;
    }
}